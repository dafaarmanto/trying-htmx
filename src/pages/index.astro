---
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Astro HTMX</title>

    <!-- HTMX -->
    <script src="/htmx.min.js" is:inline></script>
  </head>
  <body class="max-w-4xl mx-auto text-3xl pt-5">
    <form
      hx-post="/prompt"
      hx-target="#result"
      action="flex gap-2 items-center"
    >
      <div class="whitespace-nowrap text-gray-400">Input anything</div>
      <input
        type="text"
        name="prompt"
        placeholder="Prompt"
        value="Man"
        class="mt-4 border border-gray-400 p-2 rounded-lg w-full focus:outline-none focus:ring-2"
      />
      <button
        type="submit"
        class="mt-4 bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full"
        >Submit</button
      >
    </form>
    <div class="p-5 border border-gray-400 rounded-2xl mt-5">
      <div id="result"></div>
    </div>
  </body>
</html>
